services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-spring-boot
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: administradorUsuariosDB
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
